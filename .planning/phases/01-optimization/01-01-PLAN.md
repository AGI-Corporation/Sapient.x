---
phase: 01-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/agents/trade_agent.py, src/main.py, src/core/state.py]
autonomous: true
requirements: [OPTIMIZE-02, UX-01]

must_haves:
  truths:
    - "TradeAgent broadcasts offers to multiple agents in parallel"
    - "All API exceptions return standardized ErrorResponse JSON"
  artifacts:
    - path: "src/core/state.py"
      provides: "Centralized agent registry to fix circular imports"
  key_links:
    - from: "src/main.py"
      to: "src/models/parcel_models.py::ErrorResponse"
      via: "FastAPI exception handler"
---

<objective>
Refactor circular dependencies, parallelize broadcasts, and standardize error handling.

Purpose: Improve system reliability, maintainability, and scalability per GSD guidelines.
Output: Refactored core state, optimized TradeAgent, and global exception handler.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Resolve Circular Dependencies</name>
  <files>src/core/state.py, src/main.py, src/api/*.py</files>
  <action>
    Extract PARCEL_AGENTS and TRADE_AGENTS from main.py to src/core/state.py.
    Update all consumers to import from the new state module.
  </action>
  <verify>pytest passes, no ModuleNotFoundError/ImportError on startup</verify>
  <done>Registry registries live in core/state and imports are clean.</done>
</task>

<task type="auto">
  <name>Task 2: Parallelize Offer Broadcasts</name>
  <files>src/agents/trade_agent.py</files>
  <action>
    Per D-02: Update TradeAgent.broadcast_offer to use asyncio.gather for parallel delivery.
  </action>
  <verify>Integration tests for trade flow pass</verify>
  <done>Broadcasts execute concurrently instead of sequentially.</done>
</task>

<task type="auto">
  <name>Task 3: Standardize Error Responses</name>
  <files>src/main.py</files>
  <action>
    Per D-01: Implement a global FastAPI exception handler for the Exception class.
    Format responses using the ErrorResponse model (success=False, error, detail).
  </action>
  <verify>Accessing a non-existent parcel returns ErrorResponse JSON</verify>
  <done>All system errors follow a unified schema.</done>
</task>

</tasks>

<success_criteria>
- All 25 tests pass.
- API returns JSON errors even for unhandled exceptions.
- TradeAgent uses gather for broadcasts.
</success_criteria>
